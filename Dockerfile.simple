# Enkel Dockerfile for produksjon
FROM node:22-alpine

WORKDIR /app

# Kopier package files
COPY package*.json ./

# Installer alle dependencies (inkludert dev for build)
RUN npm ci

# Kopier applikasjon
COPY . .

# Bygg Next.js
RUN npm run build

# Fjern dev dependencies etter build
RUN npm prune --production

# Eksponer port
EXPOSE 3000

# Start applikasjon
CMD ["npm", "start"]